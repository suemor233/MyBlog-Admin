import {defineComponent} from 'vue'

export default defineComponent({
    name: 'category',
    setup(props, ctx) {
        return () => (
            <>
                <h1>category</h1>
            </>
        );
    }
})
